services:
  - type: web
    name: nebula-orchestrator
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
        placeholder: "Your Google Gen AI API key for Image Intelligence"
